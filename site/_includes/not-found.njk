---
layout: base.njk
---
<section class="article wrap">
  <p class="eyebrow">{{ eyebrow }}</p>
  <h1>{{ heading }}</h1>
  <div class="lead">{{ content | safe }}</div>
  <a class="text-link" href="{{ returnUrl }}">{{ returnLabel }}</a>
</section>
